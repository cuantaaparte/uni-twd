export class Operador{
    constructor(id, nombre, siglas, color, urlIcono) {
        this.operadorId = id;
        this.nombre = nombre;
        this.siglas = siglas; 
        this.color = color;   
        this.urlIcono = urlIcono;
    }
}